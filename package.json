{
	"name": "server",
	"version": "1.0.0",
	"engines": {
		"node": "14.15.4"
	},
	"description": "Server Package for Portfolio Page.",
	"main": "server.js",
	"author": "Niroshian Balasubramaniam",
	"license": "MIT",
	"dependencies": {
		"concurrently": "^6.1.0",
		"cors": "^2.8.5",
		"dotenv": "^9.0.2",
		"express": "^4.17.1",
		"nodemailer": "^6.6.0"
	},
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
	},
	"devDependencies": {
		"nodemon": "^2.0.7"
	}
}
